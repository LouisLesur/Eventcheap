<section>
  <div class="container p-5">
    <div class="d-flex flex-column">
      <h1 class="d-flex justify-content-center">Bienvenue sur Eventcheap</h1>
      <h3 class="d-flex justify-content-center">le Eventbrit du pauvre</h3>
    </div>
  </div>
</section>
<section class="">
  <div class="d-flex flex-row flex-wrap justify-content-center">
    <%= @all_events.each do |event| %>
      <div class="card m-4" style="width: 18rem;">
        <div class="card-body">
          <h5 class="card-title pl-4"><%= event.title %></h5>
          <h6 class="card-subtitle mb-2 text-muted"><%= event.start_date %></h6>
          <p class="card-text"><%= event.description %></p>
          <p class="card-text d-flex justify-content-center"><%= event.price %> €</p>
          <%= link_to"En savoir plus", event_path(event.id), :class =>"btn btn-primary"%>
        </div>
      </div>
    <%end%>
  </div>
</section>
